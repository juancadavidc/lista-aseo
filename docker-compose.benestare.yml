services:
  api:
    build: ./server
    restart: unless-stopped
    environment:
      DB_HOST: 104.131.41.153
      DB_PORT: 5432
      DB_NAME: db-benestare
      DB_USER: postgres
      DB_PASSWORD: voQLyyZ5BgFLlwmcfLaOLfSGloMQqE8AFjj0FyiGzyZrI02QoN8DLNrnLGnzECPF
      PORT: 3001
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:-cambiar-este-secreto-en-produccion}
      BETTER_AUTH_URL: http://api:3001
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
    ports:
      - "3002:3001"
    volumes:
      - uploads:/app/uploads

  web:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80"
    depends_on:
      - api

volumes:
  uploads:
